import 'package:flutter/material.dart';

class ManageUserReservations extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Manage User Reservations',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
